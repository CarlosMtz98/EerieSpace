package mx.itesm.eeriespace;

import com.badlogic.gdx.graphics.Texture;

public class GameIcon extends Objeto{

    public GameIcon(Texture textura, float x, float y) {
            super(textura, x, y);
    }
}
